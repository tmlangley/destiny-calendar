
export default class EventsController {
  constructor(events) {
    this.events = events;
  }

  /**
   * TODO: get a timestamp map of events within the given range.
   * @param range
   */
  getEvents(range) {

  }
}